# ============================================================
# 长文档 RAG 问答系统
# Fast RAG (pipeline): HyDE + Reranker + Hybrid Search
# Agentic RAG: 多轮推理 + BM25直通 + 上下文压缩
# ============================================================

# --- PDF 解析 ---
PyMuPDF>=1.24.0

# --- 嵌入（API: 硅基流动 BGE-M3, 零本地下载） ---
openai>=1.50.0

# --- 向量数据库 ---
chromadb>=0.5.0

# --- 关键词检索 ---
rank-bm25>=0.2.2

# --- 中文分词 ---
jieba>=0.42.1

# --- Reranker (本地 BGE-Reranker, Fast模式用) ---
transformers>=4.45.0
torch>=2.0.0

# --- Web 前端 ---
gradio>=6.0.0

# --- 修复 Keras 兼容性 ---
tf-keras>=2.17.0

# --- 工具 ---
tqdm>=4.66.0
numpy>=1.26.0
